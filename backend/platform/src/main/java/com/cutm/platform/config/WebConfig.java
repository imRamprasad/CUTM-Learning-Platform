package com.cutm.platform.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web configuration class.
 * Note: CORS and Security beans are configured in SecurityConfig.java
 */
@Configuration
public class WebConfig {
    // CORS and Security beans are configured in SecurityConfig.java
}
